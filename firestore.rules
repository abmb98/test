rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT RULES - Allow all access
    // For production, enable authentication and role-based access
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
